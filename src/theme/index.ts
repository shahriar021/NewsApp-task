import { colors } from './colors';
import { spacing, layout } from './spacing';
import { typography } from './typography';

export { colors, spacing, layout, typography };

export const theme = {
  colors,
  spacing,
  layout,
  typography,
};